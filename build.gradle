buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        // Android Gradle plugin.
        classpath 'com.android.tools.build:gradle:1.3.1'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}
